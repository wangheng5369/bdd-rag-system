# BDD-RAG-System 技术实现记录

## 一、项目结构图

```
bdd-rag-system/
├── docs/                           # 文档
│   ├── 用例示例.md                 # 测试用例
│   └── README.md                   # 项目架构文档
│
├── knowledge-base/                 # 知识库
│   ├── kb1_aw_sdk/               # KB1: SDK接口定义
│   │   ├── raw/                  # 原始文档
│   │   ├── chunked/              # 分片结果 (chunks.json)
│   │   └── indexed/              # ChromaDB向量数据库
│   │       └── chroma_db/
│   │
│   ├── kb2_bdd_scenarios/        # KB2: BDD场景
│   │   ├── raw/
│   │   ├── chunked/
│   │   └── indexed/
│   │       └── chroma_db/
│   │
│   └── kb3_bug_reports/          # KB3: Bug知识库
│       ├── raw/
│       ├── chunked/
│       └── indexed/
│           └── chroma_db/
│
├── core/                          # 核心模块
│   ├── retrieval/                 # 检索模块
│   │   ├── __init__.py
│   │   └── retrieval.py           # ✅ 已实现
│   ├── rerank/                   # 重排模块
│   │   ├── __init__.py
│   │   └── rerank.py            # ✅ 已实现
│   ├── llm/                      # LLM生成模块
│   │   ├── __init__.py
│   │   └── llm.py               # ✅ 已实现 (Claude API)
│   ├── step_writer/              # Step生成模块
│   │   ├── __init__.py
│   │   └── step_writer.py       # ✅ 已实现
│   ├── pipeline/                 # Pipeline 串联模块
│   │   ├── __init__.py
│   │   ├── pipeline.py          # ✅ 已实现
│   │   ├── use_case_parser.py   # ✅ 已实现
│   │   └── script_verifier.py   # ✅ 已实现 (语法+API校验)
│   ├── testing/                  # 本地测试模块
│   │   ├── __init__.py
│   │   └── testing.py            # ✅ 已实现
│   ├── skills/                   # Skill 原子化接口
│   │   ├── __init__.py          # ✅ Skill 基类 + 注册表
│   │   ├── retrieve_skill.py    # ✅ 检索 Skill
│   │   ├── generate_skill.py    # ✅ 生成 Skill (集成校验)
│   │   ├── test_skill.py        # ✅ 测试 Skill
│   │   ├── heal_skill.py        # ✅ 自愈 Skill
│   │   └── runner.py            # ✅ Skill 编排器
│   ├── verify/                   # 校验层
│   │   ├── __init__.py         # ✅ 校验数据结构
│   │   ├── llm_output_verifier.py  # ✅ LLM 输出校验
│   │   ├── feature_verifier.py     # ✅ Feature 文件校验
│   │   ├── reference_verifier.py    # ✅ Reference 引用校验
│   │   ├── constraint_verifier.py    # ✅ 约束合规校验
│   │   └── verify_pipeline.py       # ✅ 校验流水线
│   └── prompt/                   # Prompt 模板
│       └── system_prompt.md      # ✅ System Prompt
│
├── self-healing/                  # 故障自愈
│   ├── fast_path/                # 快速路径
│   │   ├── __init__.py
│   │   └── fast_path.py         # ⚠️ 部分实现
│   └── slow_path/                # 慢速路径
│       ├── __init__.py
│       └── slow_path.py         # ⏳ 待实现
│
├── scripts/                       # 嵌入脚本
│   ├── embedding_sdk.py          # ✅ KB1嵌入脚本 (AST分片)
│   ├── embedding_bdd.py         # ✅ KB2嵌入脚本 (AST分片)
│   └── embedding_bug.py        # ✅ KB3嵌入脚本 (AST分片)
│
├── config/                        # 配置文件
├── tests/                         # 测试
│   ├── test_retrieval.py         # ✅ Retrieval测试
│   └── retrieval_results.json    # 测试结果
├── main.py                        # 主入口 (Pipeline版)
├── main_skill.py                  # 主入口 (Skill版)
├── skills/                        # Skill 定义文档
│   └── bdd_pipeline_skill.md     # Skill 流程定义
└── requirements.txt               # 依赖清单
```

---

## 二、已完成阶段技术选型

### Phase 1: 知识库 Embedding

#### 知识库分片策略汇总

三个知识库均采用 **AST Chunk（基于标题层级的智能分片）** 策略，使用 `markdown-it-py` 解析 Markdown 的 AST 结构，按 `##` h2 和 `###` h3 标题层级划分chunk边界。

| 知识库 | 一级分片 (h2) | 二级分片 (h3) | 三级处理 | 分片结果 |
| --- | --- | --- | --- | --- |
| **KB1 (SDK)** | 模块边界 | 接口边界（主要检索单元） | h4 内容聚合到 h3 | 12 chunks |
| **KB2 (BDD)** | 模块边界 | 场景边界（Gherkin + Python） | — | 15 chunks |
| **KB3 (Bug)** | 模块边界 | Bug单边界（完整 bug 信息） | — | 13 chunks |

**共同点**：
- 使用 `markdown-it-py` 解析 Markdown AST
- 按 h2/h3 标题层级确定 chunk 边界
- chunk 内容包含标题路径（title_path）作为语义前缀
- 使用 LangChain `Document` 结构存储
- 使用 ChromaDB 向量数据库

**差异点**：
- **KB1**：h4 子部分内容会聚合到 h3 接口级 chunk，保证接口文档完整性
- **KB2**：h3 即为完整 Scenario，包含 Gherkin 描述和 Python 实现代码
- **KB3**：h3 是独立的 Bug 单，元数据中额外存储 `bug_id` 字段便于去重

#### 1.1 KB1 (SDK) Embedding

| 项目 | 技术选型 |
|------|----------|
| **分片策略** | AST Chunk — 按 `##` h2 和 `###` h3 标题层级分片 |
| **分片规则** | h2=模块边界，h3=接口边界（主要检索单元），h4 内容聚合到 h3 |
| **核心库** | `markdown-it-py` — Markdown AST解析 |
| **数据格式** | LangChain `Document` — 统一分片数据结构 |
| **Embedding模型** | `BAAI/bge-large-zh-v1.5` (HuggingFace) — 1024维向量 |
| **向量维度** | 1024 |
| **Embedding框架** | `langchain_community.embeddings.HuggingFaceBgeEmbeddings` |
| **向量数据库** | ChromaDB |
| **元数据解析** | `PyYAML` — 解析 frontmatter |

**分片结果**: 12 chunks (2个h2模块 + 10个h3接口)

---

#### 1.2 KB2 (BDD) Embedding

| 项目 | 技术选型 |
|------|----------|
| **分片策略** | AST Chunk — 按 `##` h2 和 `###` h3 标题层级分片 |
| **分片规则** | h2=模块边界，h3=场景边界（二级分片，每个 h3 包含 Gherkin + Python 实现） |
| **核心库** | `markdown-it-py` — Markdown AST解析 |
| **数据格式** | LangChain `Document` |
| **Embedding模型** | `BAAI/bge-m3` |
| **Embedding框架** | `langchain_community.embeddings.HuggingFaceBgeEmbeddings` |
| **向量数据库** | ChromaDB |

**分片结果**: 15 chunks (5个h2模块 + 10个h3场景)

---

#### 1.3 KB3 (Bug) Embedding

| 项目 | 技术选型 |
|------|----------|
| **分片策略** | AST Chunk — 按 `##` h2 和 `###` h3 标题层级分片 |
| **分片规则** | h2=模块边界，h3=Bug单边界（二级分片，每个 h3 包含完整 bug 信息） |
| **核心库** | `markdown-it-py` — Markdown AST解析 |
| **数据格式** | LangChain `Document` |
| **Embedding模型** | `BAAI/bge-m3` |
| **Embedding框架** | `langchain_community.embeddings.HuggingFaceBgeEmbeddings` |
| **向量数据库** | ChromaDB |

**分片结果**: 13 chunks (5个h2模块 + 8个h3 bug)

---

### Phase 2: Retrieval 检索模块

| 项目 | 技术选型 |
|------|----------|
| **向量数据库客户端** | `chromadb` (原生) / `langchain_community.vectorstores.Chroma` |
| **Embedding函数** | `langchain_community.embeddings.HuggingFaceBgeEmbeddings` |
| **LangChain接口** | `langchain_core.retrievers.BaseRetriever` — 封装为LangChain兼容Retriever |
| **数学计算** | `numpy` — 余弦相似度计算 |
| **相似度阈值** | 0.6 (可调整) |
| **降级机制** | KB2 → KB1 自动降级 |

**核心方法**:
- `retrieve(query, kb_name, top_k, threshold)` — 向量召回，带阈值过滤
- `retrieve_with_fallback(query, primary_kb, fallback_kb, top_k)` — 带降级的召回
- `compute_confidence(query, candidate)` — 计算余弦相似度
- `is_action_conflict(action1, action2)` — 动作冲突检测

---

### Phase 2.5: Hybrid Retrieval（BM25 + 向量）

**新增文件**: `core/retrieval/bm25.py`, `core/retrieval/hybrid_retriever.py`

#### 设计背景

| 检索方式 | 优点 | 适用场景 |
|----------|------|----------|
| **向量检索** | 语义理解、同义词支持 | 自然语言查询 |
| **BM25** | 精确匹配、词项权重 | API 名称、参数名、Bug ID |

#### 架构设计

```
Query
    ├── BM25 召回 → 精确匹配结果
    └── 向量召回 → 语义相似结果
            ↓
        分数归一化 + 合并
            ↓
        Rerank 精排
            ↓
        最终结果
```

#### BM25 适用场景

| 知识库 | 场景 | BM25 优势 |
|--------|------|-----------|
| **KB1 (SDK)** | `create_cluster` 函数名 | ✅ 精确匹配 |
| **KB1 (SDK)** | `data_disk=100G` 参数 | ✅ 精确匹配 |
| **KB2 (BDD)** | Step 关键词搜索 | ⚠️ 部分场景 |
| **KB3 (Bug)** | `Bug-20230815-001` ID | ✅ 精确匹配 |

#### 新增模块

| 模块 | 文件 | 说明 |
|------|------|------|
| **BM25Retriever** | `core/retrieval/bm25.py` | BM25 索引和检索 |
| **BM25Builder** | `core/retrieval/bm25.py` | 索引构建器 |
| **HybridRetriever** | `core/retrieval/hybrid_retriever.py` | 混合检索器 |
| **AdaptiveHybridRetriever** | `core/retrieval/hybrid_retriever.py` | 自适应混合检索 |

#### 使用方式

```python
from core.retrieval import HybridRetriever, BM25Retriever

# 构建 BM25 索引
from core.retrieval.bm25 import BM25Builder
bm25 = BM25Builder.build_from_chunks(chunks)

# 混合检索
hybrid = HybridRetriever(
    vector_retriever=retrieval,
    bm25_retriever=bm25,
    reranker=reranker,
    alpha=0.5  # BM25 权重
)

# 检索
results = hybrid.search(query="create_cluster", alpha=0.7)
```

#### BM25 索引构建

```bash
# 构建所有知识库的 BM25 索引
python scripts/build_bm25_index.py --kb all

# 构建指定知识库
python scripts/build_bm25_index.py --kb kb1
```

---

### Phase 4: Step级匹配 + Prompt 组装

**新增文件**: `core/pipeline/step_prompt_builder.py`

对每个用例的**每个步骤**单独进行 KB2 向量检索，根据结果组装完整 prompt：

| 步骤 | 动作 |
|------|------|
| KB2 检索 | Step 级 query，Reranker 重排 |
| 命中已有 BDD | 提取 Gherkin 填入 Context 1 |
| 命中 Then/Given 无 When | 查 KB1 补充 When API 填入 Context 2 |
| KB2 MISS | 查 KB1，资源+动作双重匹配重排，填入 Context 2 |

**新增 Action Mapping 互斥组**: `{"CREATE","UPDATE"}` 和 `{"DELETE","UPDATE"}`

---

### Phase 5: Script Verifier 脚本校验

**新增文件**: `core/pipeline/script_verifier.py`

LLM 生成脚本后、LocalTester 运行前，执行三级校验：

| 校验项 | 方法 | 失败级别 |
|--------|------|----------|
| Python 语法 | `ast.parse()` | error |
| API 签名 | 从 Context 2 提取真实签名，对比函数调用参数 | error |
| BDD 格式 | 正则检查 Given/When/Then 关键字顺序、孤立 And | warning |

**数据结构**:
```python
@dataclass
class VerificationIssue:
    severity: str  # "error" | "warning"
    check: str    # "syntax" | "api" | "bdd"
    message: str
    location: str  # "line 42"
```

---

## 三、技术栈汇总

### 开源库

| 库 | 版本 | 用途 |
|----|------|------|
| `markdown-it-py` | latest | Markdown AST解析 |
| `PyYAML` | latest | YAML frontmatter解析 |
| `numpy` | latest | 数学计算 |
| `chromadb` | latest | 向量数据库 |
| `langchain-core` | ~0.1.x | BaseRetriever抽象 |
| `langchain-community` | ~0.0.x | ChromaDB连接、HuggingFaceBgeEmbeddings |
| `sentence-transformers` | latest | BGE-M3模型加载 |

### 预训练模型

| 模型 | 来源 | 维度 | 用途 |
|------|------|------|------|
| `BAAI/bge-large-zh-v1.5` | HuggingFace | 1024 | 中文Embedding |

### LangChain 组件

```
LangChain
├── langchain_core
│   ├── documents.py        # Document 数据结构
│   └── retrievers.py       # BaseRetriever 抽象类
└── langchain_community
    ├── embeddings/
    │   └── huggingface.py  # HuggingFaceBgeEmbeddings (deprecated)
    └── vectorstores/
        └── chroma.py        # Chroma 向量数据库
```

---

## 四、核心代码片段

### 4.1 AST分片 (markdown-it-py)

```python
from markdown_it import MarkdownIt

md = MarkdownIt()
tokens = md.parse(markdown_text)

# 第一遍：收集 h2/h3 标题信息
headings = []
for i, token in enumerate(tokens):
    if token.type == "heading_open":
        level = int(token.tag[1])
        if level not in (2, 3):
            continue
        # 提取标题内容
        heading_content = ""
        for j in range(i + 1, len(tokens)):
            if tokens[j].type == "inline":
                heading_content = tokens[j].content.strip()
                break
        headings.append({
            "level": level,
            "line": token.map[0],
            "content": heading_content
        })

# 第二遍：按行分片
lines = content.split("\n")
for idx, heading in enumerate(headings):
    start_line = heading["line"]
    # 找下一个同级/上级标题确定结束位置
    end_line = len(lines)
    for next_h in headings[idx + 1:]:
        if next_h["level"] <= heading["level"]:
            end_line = next_h["line"]
            break
    chunk_content = "\n".join(lines[start_line + 1:end_line])
```

### 4.2 Retrieval 召回

```python
from langchain_community.vectorstores import Chroma

vectorstore = Chroma(
    persist_directory=persist_dir,
    embedding_function=embeddings,
    collection_name="cce_sdk_kb1"
)

# 相似度搜索
results = vectorstore.similarity_search_with_score(query, k=top_k)

# 过滤低于阈值的结果
threshold = 0.6
recalled = [
    {"score": float(score), "content": doc.page_content, "metadata": doc.metadata}
    for doc, score in results
    if float(score) >= threshold
]
```

### 4.3 LangChain BaseRetriever 封装

```python
from langchain_core.retrievers import BaseRetriever
from langchain_core.documents import Document

class BGERetriever(BaseRetriever):
    def __init__(self, vectorstore, top_k=5):
        self.vectorstore = vectorstore
        self.top_k = top_k

    def _get_relevant_documents(self, query: str) -> List[Document]:
        results = self.vectorstore.similarity_search(query, k=self.top_k)
        return results
```

---

## 五、依赖清单

```txt
# Core RAG
chromadb>=0.4.0
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10

# Embedding & Model
sentence-transformers>=2.2.0

# Text Processing
markdown-it-py>=0.8.0
PyYAML>=6.0
numpy>=1.24.0

# Optional (待实现阶段需要)
# anthropic>=0.8.0          # LLM调用
# flagembedding>=1.0.0    # bge-reranker
```

---

## 六、已完成阶段状态

| Phase | 内容 | 状态 |
|-------|------|------|
| Phase 1 | 知识库 Embedding | ✅ 已实现 |
| Phase 2 | Retrieval 检索模块 | ✅ 已实现 |
| Phase 3 | Reranker 重排模块 | ✅ 已实现 |
| Phase 4 | LLM Generator + StepWriter | ✅ 已实现 |
| Phase 5 | Script Verifier 脚本校验 | ✅ 已实现 |
| Phase 6 | LocalTester | ✅ 已实现 |
| Phase 7 | Fast Path 自愈 | ✅ 已实现 |
| Phase 8 | Slow Path 自愈 | ✅ 已实现 |
| Phase 9 | System Prompt 设计 | ✅ 已实现 |
| Phase 10 | Skill 原子化接口 | ✅ 已实现 |
| Phase 11 | Skill Runner 编排器 | ✅ 已实现 |

---

## 七、System Prompt 设计

### 设计思路

```
┌─────────────────────────────────────────────────────────────┐
│  System Prompt (持久化，一次定义，所有调用共享)              │
├─────────────────────────────────────────────────────────────┤
│  # Role                                                    │
│  # 专业能力                                                │
│  # 绝对约束                                                │
│  # 输出格式规范                                            │
│  # 质量检查清单                                            │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  User Prompt (每次调用只传任务相关内容)                     │
├─────────────────────────────────────────────────────────────┤
│  # Task                                                    │
│  # Context 1: 已有的 BDD 步骤（含 ID）                     │
│  # Context 2: 可用的 SDK API（含 ID）                      │
│  # Context 3: 原始用例                                     │
│  # 输出要求                                                │
└─────────────────────────────────────────────────────────────┘
```

### System Prompt 文件

```
core/prompt/system_prompt.md
```

### System Prompt 内容

| 章节 | 内容 |
|------|------|
| Role | K8s/CCE 自动化测试专家，BDD + pytest-bdd |
| 专业能力 | Gherkin 语法、pytest-bdd、云平台 API |
| 绝对约束 | API 约束、BDD 复用约束、输出格式约束、缺失 API 处理 |
| Reference 机制 | Step 来源追踪（Type/Source/ID） |
| 输出格式 | .feature 文件、.py 文件、Reference 映射表 |
| 质量检查 | 自检清单 |

### User Prompt 设计

| Context | 内容 | 格式 |
|---------|------|------|
| Context 1 | 已有的 BDD 步骤 | 表格（含 ID） |
| Context 2 | 可用的 SDK API | 表格（含 ID + 签名） |
| Context 3 | 原始文本用例 | 结构化文本 |

---

## 八、Skill 原子化接口设计

### 4 个核心 Skill

| Skill | Name | 描述 | 输入 | 输出 |
|-------|------|------|------|------|
| **RetrieveSkill** | `retrieve` | 从 KB 检索 | query | KB1/KB2/KB3 召回结果 |
| **GenerateSkill** | `generate` | LLM 生成代码 | use_case | Feature + Python 文件 |
| **TestSkill** | `test` | 运行测试 | script_path | 测试结果 |
| **HealSkill** | `heal` | 自愈修复 | error_message | fix_content |

### Skill 统一接口

```python
class BaseSkill(ABC):
    name: str              # 路由名称
    description: str       # 描述
    params_schema: Dict    # 参数校验

    def execute(self, params: Dict) -> SkillResult:
        """统一执行入口"""
        pass

@dataclass
class SkillResult:
    success: bool              # 是否成功
    data: Any                 # 返回数据
    error: Optional[str]       # 错误信息
    metadata: Dict[str, Any]  # 元数据
```

### Skill 编排器 (Runner)

```python
class SkillRunner:
    def add_stage(self, skill_name: str) -> "SkillRunner":
        """添加阶段"""

    def run(self, initial_params: Dict) -> Dict[str, StageResult]:
        """执行 Pipeline"""

class Pipelines:
    @staticmethod
    def full() -> SkillRunner:
        """完整 Pipeline: retrieve → generate → test → heal"""

    @staticmethod
    def generate_and_test() -> SkillRunner:
        """生成+测试: generate → test"""
```

### 调用示例

```python
from core.skills import SkillRunner, Pipelines, get_skill

# 完整 Pipeline
runner = Pipelines.full()
results = runner.run({"use_case": use_case})

# 按需组合
runner = SkillRunner()
runner.add_stage("retrieve").add_stage("generate").add_stage("test")
results = runner.run({"use_case": use_case})

# 单独调用
skill = get_skill("retrieve")
result = skill.execute({"query": "纳管节点", "kb": "all"})
```

### Skill 文件结构

```
core/skills/
├── __init__.py           # Skill 基类 + 注册表
├── README.md              # 使用文档
├── retrieve_skill.py      # 检索 Skill
├── generate_skill.py      # 生成 Skill
├── test_skill.py          # 测试 Skill
├── heal_skill.py          # 自愈 Skill
└── runner.py              # Skill 编排器

core/prompt/
└── system_prompt.md      # System Prompt 定义
```

---

## 九、Reference 机制

### 设计目的

1. **溯源**：追踪每个 step 的知识库来源
2. **自愈**：测试失败时快速定位 KB 条目
3. **去重**：避免重复生成相似 step

### Reference 字段

| 字段 | 值 | 说明 |
|------|-----|------|
| **Type** | `existing_bdd` / `new_sdk` / `param_config` / `missing` | step 类型 |
| **Source** | `kb1` / `kb2` / `kb3` | 知识库编号 |
| **ID** | `chunk_xxx` / `api_xxx` | 具体条目标识 |

### Reference 映射表示例

```markdown
## Reference
| Step | Type | Source | ID |
|------|------|--------|-----|
| Given 已存在可用集群 | existing_bdd | kb2 | chunk_cluster_001 |
| When 构造创建ECS虚机请求 | new_sdk | kb1 | api_create_ecs |
| And 设置数据盘为100G | param_config | kb1 | api_create_ecs |
| When 纳管ECS虚机到CCE集群 | new_sdk | kb1 | api_join_cluster |
```

---

## 十、主入口

### Pipeline 版 (main.py)

```bash
python main.py --use-case-file docs/用例示例.md
```

### Skill 版 (main_skill.py)

```bash
# 完整 Pipeline
python main_skill.py --pipeline full

# 仅生成
python main_skill.py --pipeline generate_only

# 生成+测试
python main_skill.py --pipeline generate_and_test
```

---

## 十一、校验层设计（Verify Module）

### 设计目的

在 LLM 生成用例后、提交 PR 前，进行多维度校验，确保：
1. **格式正确** - Feature/Python/Reference 能被正确解析
2. **内容合规** - 符合 System Prompt 约束
3. **引用有效** - Reference 指向的 KB 条目真实存在
4. **可执行** - Python 语法 + API 签名正确

### 校验流程

```
LLM 输出
    ↓
┌─────────────────────────────────────────────┐
│ 1. OutputVerifier - LLM 输出校验          │
│    - 输出是否为空                          │
│    - 是否包含禁止内容                      │
│    - 格式是否完整                          │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 2. FeatureVerifier - Feature 文件校验       │
│    - Gherkin 语法                          │
│    - 关键字完整性                          │
│    - Step 顺序合法                        │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 3. ReferenceVerifier - Reference 引用校验   │
│    - ID 格式校验                          │
│    - KB 条目存在性                        │
│    - Step 与 Reference 匹配                │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 4. ConstraintVerifier - 约束合规校验        │
│    - API 都在 Context 2 白名单内          │
│    - 无虚构 API                           │
│    - BDD 语句原封不动复用                  │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│ 5. ScriptVerifier - 已有静态校验            │
│    - Python 语法                          │
│    - API 签名                            │
│    - BDD 格式                            │
└─────────────────────────────────────────────┘
    ↓
通过 → 可提交 PR
失败 → 返回错误列表，要求 LLM 重生成
```

### 校验器一览

| 校验器 | 文件 | 检查点 |
|--------|------|--------|
| **LLMOutputVerifier** | llm_output_verifier.py | 空输出、禁止内容、格式完整性 |
| **FeatureVerifier** | feature_verifier.py | Gherkin 语法、关键字、Step 顺序 |
| **ReferenceVerifier** | reference_verifier.py | ID 格式、KB 存在性、Step 匹配 |
| **ConstraintVerifier** | constraint_verifier.py | API 白名单、复用约束、解释内容 |
| **ScriptVerifier** | script_verifier.py | Python 语法、API 签名、BDD 格式 |

### 校验结果

```python
@dataclass
class VerificationReport:
    passed: bool
    stage_results: Dict[str, VerificationResult]
    error_count: int
    warning_count: int

    def can_submit(self) -> bool:
        """是否可以提交 PR"""
        return self.passed and self.error_count == 0

    def summary(self) -> str:
        """生成摘要"""
```

### 校验调用示例

```python
from core.verify import VerifyPipeline, quick_verify

# 方式1：使用 VerifyPipeline（完整校验）
pipeline = VerifyPipeline(retrieval=retrieval)
report = pipeline.verify(
    raw_llm_output=llm_output,
    feature_content=feature,
    python_content=python,
    reference_table=reference,
    kb_results=kb_results,
    context1_bdd=existing_bdds,
    context2_apis=available_apis
)
print(report.summary())
print("可以提交:", report.can_submit())

# 方式2：使用快捷函数（基础校验）
report = quick_verify(feature, python, reference)
```

### 集成到 GenerateSkill

GenerateSkill 在 LLM 生成后自动执行校验，结果包含在 metadata 中：

```python
result = generate.execute({"use_case": use_case})
print(result.metadata["verification"])  # 校验报告摘要
print(result.metadata["can_submit"])   # 是否可提交
```
